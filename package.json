{
  "name": "gridgen-sandbox",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist -b dist"
  },
  "dependencies": {
    "pixi.js": "^8.2.6"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.4.0"
  }
}
